<Border x:Class="JustView.TextViewer"
        x:Name="Root"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:JustView"
        Background="{DynamicResource Gar.Brush.WindowBackground}">
    <Grid>
        <TextBox x:Name="TextBoxView"
                 BorderThickness="0"
                 Background="{Binding ElementName=Root, Path=Background}"
                 Foreground="{DynamicResource Gar.Brush.WindowText}"
                 CaretBrush="{DynamicResource Gar.Brush.WindowText}"
                 SelectionBrush="{DynamicResource Gar.Brush.TextSelectionBackground}"
                 SelectionTextBrush="{DynamicResource Gar.Brush.TextSelectionText}"
                 FontFamily="Consolas, Courier New"
                 FontSize="12"
                 Padding="2"
                 IsReadOnly="True"
                 IsUndoEnabled="False"
                 AcceptsReturn="True"
                 AcceptsTab="True"
                 TextWrapping="NoWrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"/>
        <ListBox x:Name="VirtualTextView"
                 Visibility="Collapsed"
                 BorderThickness="0"
                 Background="{Binding ElementName=Root, Path=Background}"
                 Foreground="{DynamicResource Gar.Brush.WindowText}"
                 FontFamily="Consolas, Courier New"
                 FontSize="12"
                 ScrollViewer.CanContentScroll="True"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 VirtualizingStackPanel.IsVirtualizing="True"
                 VirtualizingStackPanel.VirtualizationMode="Recycling">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemContainerStyle>
                <Style TargetType="{x:Type ListBoxItem}">
                    <Setter Property="Focusable" Value="False"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}"
                               Foreground="{DynamicResource Gar.Brush.WindowText}"
                               FontFamily="{Binding FontFamily, RelativeSource={RelativeSource AncestorType={x:Type ListBox}}}"
                               FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type ListBox}}}"
                               Padding="2,0,2,0"
                               TextWrapping="{Binding VirtualTextWrapping, RelativeSource={RelativeSource AncestorType={x:Type local:TextViewer}}}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Border>
