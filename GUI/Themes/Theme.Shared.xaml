<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:GARbro.GUI"
                    xmlns:primitives="clr-namespace:System.Windows.Controls.Primitives;assembly=PresentationFramework"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">
    <Style TargetType="{x:Type Window}">
        <Setter Property="Background" Value="{DynamicResource Gar.Brush.ControlBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlText}"/>
        <Setter Property="TextElement.Foreground" Value="{DynamicResource Gar.Brush.ControlText}"/>
    </Style>

    <Style TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlText}"/>
    </Style>

    <Style TargetType="{x:Type Label}">
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlText}"/>
    </Style>

    <Style TargetType="{x:Type Hyperlink}">
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.Hyperlink}"/>
    </Style>

    <Style TargetType="{x:Type TextBox}">
        <Setter Property="Background" Value="{DynamicResource Gar.Brush.InputBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlText}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Gar.Brush.ControlBorder}"/>
        <Setter Property="CaretBrush" Value="{DynamicResource Gar.Brush.ControlText}"/>
        <Setter Property="SelectionBrush" Value="{DynamicResource Gar.Brush.TextSelectionBackground}"/>
        <Setter Property="SelectionTextBrush" Value="{DynamicResource Gar.Brush.TextSelectionText}"/>
    </Style>

    <Style TargetType="{x:Type RichTextBox}">
        <Setter Property="Background" Value="{DynamicResource Gar.Brush.InputBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlText}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Gar.Brush.ControlBorder}"/>
        <Setter Property="CaretBrush" Value="{DynamicResource Gar.Brush.ControlText}"/>
        <Setter Property="SelectionBrush" Value="{DynamicResource Gar.Brush.TextSelectionBackground}"/>
        <Setter Property="SelectionTextBrush" Value="{DynamicResource Gar.Brush.TextSelectionText}"/>
    </Style>

    <Style TargetType="{x:Type local:ExtAutoCompleteBox}">
        <Setter Property="Background" Value="{DynamicResource Gar.Brush.InputBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlText}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Gar.Brush.ControlBorder}"/>
    </Style>

    <Style TargetType="{x:Type Button}">
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlText}"/>
        <Setter Property="Background" Value="{DynamicResource Gar.Brush.ButtonBackground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Gar.Brush.ControlBorder}"/>
        <Setter Property="Padding" Value="4,1"/>
    </Style>

    <Style TargetType="{x:Type ToggleButton}">
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlText}"/>
        <Setter Property="Background" Value="{DynamicResource Gar.Brush.ButtonBackground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Gar.Brush.ControlBorder}"/>
    </Style>

    <Style TargetType="{x:Type CheckBox}">
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlText}"/>
    </Style>

    <Style TargetType="{x:Type RadioButton}">
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlText}"/>
    </Style>

    <Style TargetType="{x:Type ComboBox}">
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlText}"/>
        <Setter Property="Background" Value="{DynamicResource Gar.Brush.InputBackground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Gar.Brush.ControlBorder}"/>
        <Setter Property="Padding" Value="4,1"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
        <Style.Triggers>
            <Trigger Property="IsEditable" Value="False">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ComboBox}">
                            <Grid SnapsToDevicePixels="True">
                                <ToggleButton x:Name="ToggleButton"
                                              BorderBrush="{TemplateBinding BorderBrush}"
                                              Background="{TemplateBinding Background}"
                                              Foreground="{TemplateBinding Foreground}"
                                              Focusable="False"
                                              ClickMode="Press"
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                                            <Border x:Name="ToggleBorder"
                                                    Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="1">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition/>
                                                        <ColumnDefinition Width="22"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Path Grid.Column="1"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"
                                                          Data="M 0,0 L 4,4 L 8,0 Z"
                                                          Fill="{DynamicResource Gar.Brush.IconMuted}"/>
                                                </Grid>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="ToggleBorder" Property="Background" Value="{DynamicResource Gar.Brush.ListHoverBackground}"/>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter TargetName="ToggleBorder" Property="Background" Value="{DynamicResource Gar.Brush.ListInactiveSelectedBackground}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <TextBlock x:Name="ContentSite"
                                           Margin="{TemplateBinding Padding}"
                                           Padding="0,0,22,0"
                                           HorizontalAlignment="Left"
                                           VerticalAlignment="Center"
                                           IsHitTestVisible="False"
                                           Foreground="{TemplateBinding Foreground}"
                                           Text="{TemplateBinding Text}"
                                           TextTrimming="CharacterEllipsis"/>
                                <Popup x:Name="PART_Popup"
                                       Placement="Bottom"
                                       AllowsTransparency="True"
                                       Focusable="False"
                                       IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}">
                                    <Grid MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                          MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                        <Border Background="{DynamicResource Gar.Brush.WindowBackground}"
                                                BorderBrush="{DynamicResource Gar.Brush.ControlBorder}"
                                                BorderThickness="1"/>
                                        <ScrollViewer Margin="1">
                                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Grid>
                                </Popup>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="HasItems" Value="False">
                                    <Setter TargetName="PART_Popup" Property="MinHeight" Value="20"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlDisabledText}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
            <Trigger Property="IsEditable" Value="True">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ComboBox}">
                            <Grid SnapsToDevicePixels="True">
                                <ToggleButton x:Name="ToggleButton"
                                              BorderBrush="{TemplateBinding BorderBrush}"
                                              Background="{TemplateBinding Background}"
                                              Foreground="{TemplateBinding Foreground}"
                                              Focusable="False"
                                              ClickMode="Press"
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                                            <Border x:Name="ToggleBorder"
                                                    Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="1">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition/>
                                                        <ColumnDefinition Width="22"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Path Grid.Column="1"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"
                                                          Data="M 0,0 L 4,4 L 8,0 Z"
                                                          Fill="{DynamicResource Gar.Brush.IconMuted}"/>
                                                </Grid>
                                            </Border>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <TextBox x:Name="PART_EditableTextBox"
                                         Margin="3,1,24,1"
                                         Padding="0"
                                         HorizontalContentAlignment="Left"
                                         VerticalContentAlignment="Center"
                                         Background="Transparent"
                                         Foreground="{TemplateBinding Foreground}"
                                         BorderThickness="0"
                                         IsReadOnly="{TemplateBinding IsReadOnly}"/>
                                <Popup x:Name="PART_Popup"
                                       Placement="Bottom"
                                       AllowsTransparency="True"
                                       Focusable="False"
                                       IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}">
                                    <Grid MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                          MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                        <Border Background="{DynamicResource Gar.Brush.WindowBackground}"
                                                BorderBrush="{DynamicResource Gar.Brush.ControlBorder}"
                                                BorderThickness="1"/>
                                        <ScrollViewer Margin="1">
                                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Grid>
                                </Popup>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                    <Setter TargetName="ToggleButton" Property="BorderBrush" Value="{DynamicResource Gar.Brush.FocusOutline}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlDisabledText}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="{x:Type ComboBoxItem}">
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlText}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="4,2"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                    <Border x:Name="ItemBorder"
                            Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter ContentSource="Content"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource Gar.Brush.ListHoverBackground}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource Gar.Brush.ListSelectedBackground}"/>
                            <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ListSelectedText}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlDisabledText}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type GroupBox}">
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlText}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Gar.Brush.ControlBorder}"/>
    </Style>

    <Style TargetType="{x:Type ListBox}">
        <Setter Property="Background" Value="{DynamicResource Gar.Brush.ListBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ListText}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Gar.Brush.ControlBorder}"/>
    </Style>

    <Style TargetType="{x:Type ListView}">
        <Setter Property="Background" Value="{DynamicResource Gar.Brush.ListBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ListText}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Gar.Brush.ControlBorder}"/>
    </Style>

    <Style TargetType="{x:Type ListBoxItem}">
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ListText}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Gar.Brush.ListHoverBackground}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ListSelectedText}"/>
                <Setter Property="Background" Value="{DynamicResource Gar.Brush.ListSelectedBackground}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="{x:Type ListViewItem}" BasedOn="{StaticResource {x:Type ListBoxItem}}">
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
    </Style>

    <Style x:Key="Gar.GridViewColumnHeaderGripper" TargetType="{x:Type primitives:Thumb}">
        <Setter Property="Width" Value="8"/>
        <Setter Property="Cursor" Value="SizeWE"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type primitives:Thumb}">
                    <Border Background="Transparent"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type GridViewColumnHeader}">
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlText}"/>
        <Setter Property="Background" Value="{DynamicResource Gar.Brush.ControlBackground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Gar.Brush.ControlBorder}"/>
        <Setter Property="Padding" Value="6,2"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type GridViewColumnHeader}">
                    <Grid SnapsToDevicePixels="True">
                        <Border x:Name="HeaderBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="0,0,1,1">
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <primitives:Thumb x:Name="PART_HeaderGripper"
                                          HorizontalAlignment="Right"
                                          Margin="0,0,-4,0"
                                          Style="{StaticResource Gar.GridViewColumnHeaderGripper}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="HeaderBorder" Property="Background" Value="{DynamicResource Gar.Brush.ListHoverBackground}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="HeaderBorder" Property="Background" Value="{DynamicResource Gar.Brush.ListInactiveSelectedBackground}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlDisabledText}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type TreeView}">
        <Setter Property="Background" Value="{DynamicResource Gar.Brush.WindowBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.WindowText}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Gar.Brush.ControlBorder}"/>
    </Style>

    <Style TargetType="{x:Type TreeViewItem}">
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.WindowText}"/>
    </Style>

    <Style TargetType="{x:Type Menu}">
        <Setter Property="Background" Value="{DynamicResource Gar.Brush.MenuBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.MenuText}"/>
    </Style>

    <Style TargetType="{x:Type ContextMenu}">
        <Setter Property="Background" Value="{DynamicResource Gar.Brush.MenuBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.MenuText}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Gar.Brush.ControlBorder}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ContextMenu}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1"
                            Padding="2"
                            SnapsToDevicePixels="True">
                        <StackPanel IsItemsHost="True"
                                    KeyboardNavigation.DirectionalNavigation="Cycle"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="Gar.TopLevelHeaderMenuItemTemplate" TargetType="{x:Type MenuItem}">
        <Grid SnapsToDevicePixels="True">
            <Border x:Name="ItemBorder"
                    Background="{TemplateBinding Background}"
                    BorderBrush="Transparent"
                    BorderThickness="1"
                    Padding="{TemplateBinding Padding}">
                <ContentPresenter ContentSource="Header"
                                  RecognizesAccessKey="True"
                                  VerticalAlignment="Center"/>
            </Border>
            <Popup x:Name="PART_Popup"
                   AllowsTransparency="True"
                   Focusable="False"
                   IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                   Placement="Bottom"
                   PopupAnimation="None">
                <Border Background="{DynamicResource Gar.Brush.MenuBackground}"
                        BorderBrush="{DynamicResource Gar.Brush.ControlBorder}"
                        BorderThickness="1"
                        Padding="2"
                        SnapsToDevicePixels="True">
                    <StackPanel IsItemsHost="True"
                                KeyboardNavigation.DirectionalNavigation="Cycle"/>
                </Border>
            </Popup>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource Gar.Brush.ListHoverBackground}"/>
                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{DynamicResource Gar.Brush.FocusOutline}"/>
            </Trigger>
            <Trigger Property="IsSubmenuOpen" Value="True">
                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource Gar.Brush.ListHoverBackground}"/>
                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{DynamicResource Gar.Brush.FocusOutline}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlDisabledText}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="Gar.TopLevelItemMenuItemTemplate" TargetType="{x:Type MenuItem}">
        <Border x:Name="ItemBorder"
                Background="{TemplateBinding Background}"
                BorderBrush="Transparent"
                BorderThickness="1"
                Padding="{TemplateBinding Padding}"
                SnapsToDevicePixels="True">
            <ContentPresenter ContentSource="Header"
                              RecognizesAccessKey="True"
                              VerticalAlignment="Center"/>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource Gar.Brush.ListHoverBackground}"/>
                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{DynamicResource Gar.Brush.FocusOutline}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlDisabledText}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="Gar.SubmenuItemMenuItemTemplate" TargetType="{x:Type MenuItem}">
        <Border x:Name="ItemBorder"
                Background="{TemplateBinding Background}"
                BorderBrush="Transparent"
                BorderThickness="1"
                Padding="{TemplateBinding Padding}"
                SnapsToDevicePixels="True">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="22"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="16"/>
                </Grid.ColumnDefinitions>
                <Path x:Name="CheckMark"
                      Grid.Column="0"
                      Width="10"
                      Height="8"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      Data="M 1,4 L 4,7 L 9,1"
                      Stroke="{DynamicResource Gar.Brush.MenuText}"
                      StrokeThickness="1.5"
                      Visibility="Collapsed"/>
                <ContentPresenter Grid.Column="1"
                                  ContentSource="Header"
                                  Margin="2,0,18,0"
                                  RecognizesAccessKey="True"
                                  VerticalAlignment="Center"/>
                <TextBlock Grid.Column="2"
                           Text="{TemplateBinding InputGestureText}"
                           Foreground="{DynamicResource Gar.Brush.IconMuted}"
                           Margin="18,0,4,0"
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource Gar.Brush.ListHoverBackground}"/>
                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{DynamicResource Gar.Brush.FocusOutline}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlDisabledText}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="Gar.SubmenuHeaderMenuItemTemplate" TargetType="{x:Type MenuItem}">
        <Grid SnapsToDevicePixels="True">
            <Border x:Name="ItemBorder"
                    Background="{TemplateBinding Background}"
                    BorderBrush="Transparent"
                    BorderThickness="1"
                    Padding="{TemplateBinding Padding}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="22"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="16"/>
                    </Grid.ColumnDefinitions>
                    <Path x:Name="CheckMark"
                          Grid.Column="0"
                          Width="10"
                          Height="8"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Data="M 1,4 L 4,7 L 9,1"
                          Stroke="{DynamicResource Gar.Brush.MenuText}"
                          StrokeThickness="1.5"
                          Visibility="Collapsed"/>
                    <ContentPresenter Grid.Column="1"
                                      ContentSource="Header"
                                      Margin="2,0,18,0"
                                      RecognizesAccessKey="True"
                                      VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="2"
                               Text="{TemplateBinding InputGestureText}"
                               Foreground="{DynamicResource Gar.Brush.IconMuted}"
                               Margin="18,0,4,0"
                               VerticalAlignment="Center"/>
                    <Path Grid.Column="3"
                          Width="5"
                          Height="8"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Data="M 0,0 L 4,4 L 0,8 Z"
                          Fill="{DynamicResource Gar.Brush.IconMuted}"/>
                </Grid>
            </Border>
            <Popup x:Name="PART_Popup"
                   AllowsTransparency="True"
                   Focusable="False"
                   HorizontalOffset="-2"
                   IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                   Placement="Right"
                   PopupAnimation="None"
                   VerticalOffset="-3">
                <Border Background="{DynamicResource Gar.Brush.MenuBackground}"
                        BorderBrush="{DynamicResource Gar.Brush.ControlBorder}"
                        BorderThickness="1"
                        Padding="2"
                        SnapsToDevicePixels="True">
                    <StackPanel IsItemsHost="True"
                                KeyboardNavigation.DirectionalNavigation="Cycle"/>
                </Border>
            </Popup>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource Gar.Brush.ListHoverBackground}"/>
                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{DynamicResource Gar.Brush.FocusOutline}"/>
            </Trigger>
            <Trigger Property="IsSubmenuOpen" Value="True">
                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource Gar.Brush.ListHoverBackground}"/>
                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{DynamicResource Gar.Brush.FocusOutline}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlDisabledText}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="{x:Type MenuItem}">
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.MenuText}"/>
        <Setter Property="Background" Value="{DynamicResource Gar.Brush.MenuBackground}"/>
        <Setter Property="Padding" Value="8,3"/>
        <Setter Property="Template" Value="{StaticResource Gar.SubmenuItemMenuItemTemplate}"/>
        <Style.Triggers>
            <Trigger Property="Role" Value="TopLevelHeader">
                <Setter Property="Template" Value="{StaticResource Gar.TopLevelHeaderMenuItemTemplate}"/>
                <Setter Property="Padding" Value="8,2"/>
            </Trigger>
            <Trigger Property="Role" Value="TopLevelItem">
                <Setter Property="Template" Value="{StaticResource Gar.TopLevelItemMenuItemTemplate}"/>
                <Setter Property="Padding" Value="8,2"/>
            </Trigger>
            <Trigger Property="Role" Value="SubmenuHeader">
                <Setter Property="Template" Value="{StaticResource Gar.SubmenuHeaderMenuItemTemplate}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlDisabledText}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="{x:Type StatusBar}">
        <Setter Property="Background" Value="{DynamicResource Gar.Brush.StatusBarBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlText}"/>
    </Style>

    <Style TargetType="{x:Type StatusBarItem}">
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlText}"/>
    </Style>

    <Style TargetType="{x:Type TabControl}">
        <Setter Property="Background" Value="{DynamicResource Gar.Brush.ControlBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlText}"/>
    </Style>

    <Style TargetType="{x:Type TabItem}">
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlText}"/>
        <Setter Property="Background" Value="{DynamicResource Gar.Brush.ControlBackground}"/>
    </Style>

    <Style TargetType="{x:Type Expander}">
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ControlText}"/>
        <Setter Property="Background" Value="{DynamicResource Gar.Brush.ControlBackground}"/>
    </Style>

    <Style x:Key="Gar.ScrollbarLineButton" TargetType="{x:Type RepeatButton}">
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="Background" Value="{DynamicResource Gar.Brush.ScrollbarTrack}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Gar.Brush.ControlBorder}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="0">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource Gar.Brush.ListHoverBackground}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource Gar.Brush.ListInactiveSelectedBackground}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Gar.ScrollbarPageButton" TargetType="{x:Type RepeatButton}">
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border Background="{TemplateBinding Background}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Gar.ScrollbarThumb" TargetType="{x:Type primitives:Thumb}">
        <Setter Property="Background" Value="{DynamicResource Gar.Brush.ScrollbarThumb}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type primitives:Thumb}">
                    <Border x:Name="ThumbBorder"
                            Margin="2"
                            CornerRadius="3"
                            Background="{TemplateBinding Background}"/>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ThumbBorder" Property="Background" Value="{DynamicResource Gar.Brush.ScrollbarThumbHover}"/>
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="ThumbBorder" Property="Background" Value="{DynamicResource Gar.Brush.ScrollbarThumbPressed}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ScrollBar}">
        <Setter Property="Background" Value="{DynamicResource Gar.Brush.ScrollbarTrack}"/>
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.IconMuted}"/>
        <Setter Property="Width" Value="18"/>
        <Setter Property="MinWidth" Value="18"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollBar}">
                    <Grid Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="18"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="18"/>
                        </Grid.RowDefinitions>
                        <RepeatButton Grid.Row="0"
                                      Command="{x:Static ScrollBar.LineUpCommand}"
                                      Style="{StaticResource Gar.ScrollbarLineButton}">
                            <Path Data="M 0,5 L 4,1 L 8,5 Z" Fill="{TemplateBinding Foreground}"/>
                        </RepeatButton>
                        <primitives:Track x:Name="PART_Track" Grid.Row="1" IsDirectionReversed="True">
                            <primitives:Track.DecreaseRepeatButton>
                                <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}"
                                              Style="{StaticResource Gar.ScrollbarPageButton}"/>
                            </primitives:Track.DecreaseRepeatButton>
                            <primitives:Track.Thumb>
                                <primitives:Thumb Style="{StaticResource Gar.ScrollbarThumb}"/>
                            </primitives:Track.Thumb>
                            <primitives:Track.IncreaseRepeatButton>
                                <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}"
                                              Style="{StaticResource Gar.ScrollbarPageButton}"/>
                            </primitives:Track.IncreaseRepeatButton>
                        </primitives:Track>
                        <RepeatButton Grid.Row="2"
                                      Command="{x:Static ScrollBar.LineDownCommand}"
                                      Style="{StaticResource Gar.ScrollbarLineButton}">
                            <Path Data="M 0,1 L 4,5 L 8,1 Z" Fill="{TemplateBinding Foreground}"/>
                        </RepeatButton>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Width" Value="{x:Static sys:Double.NaN}"/>
                <Setter Property="MinWidth" Value="0"/>
                <Setter Property="Height" Value="18"/>
                <Setter Property="MinHeight" Value="18"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ScrollBar}">
                            <Grid Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="18"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="18"/>
                                </Grid.ColumnDefinitions>
                                <RepeatButton Grid.Column="0"
                                              Command="{x:Static ScrollBar.LineLeftCommand}"
                                              Style="{StaticResource Gar.ScrollbarLineButton}">
                                    <Path Data="M 5,0 L 1,4 L 5,8 Z" Fill="{TemplateBinding Foreground}"/>
                                </RepeatButton>
                                <primitives:Track x:Name="PART_Track" Grid.Column="1">
                                    <primitives:Track.DecreaseRepeatButton>
                                        <RepeatButton Command="{x:Static ScrollBar.PageLeftCommand}"
                                                      Style="{StaticResource Gar.ScrollbarPageButton}"/>
                                    </primitives:Track.DecreaseRepeatButton>
                                    <primitives:Track.Thumb>
                                        <primitives:Thumb Style="{StaticResource Gar.ScrollbarThumb}"/>
                                    </primitives:Track.Thumb>
                                    <primitives:Track.IncreaseRepeatButton>
                                        <RepeatButton Command="{x:Static ScrollBar.PageRightCommand}"
                                                      Style="{StaticResource Gar.ScrollbarPageButton}"/>
                                    </primitives:Track.IncreaseRepeatButton>
                                </primitives:Track>
                                <RepeatButton Grid.Column="2"
                                              Command="{x:Static ScrollBar.LineRightCommand}"
                                              Style="{StaticResource Gar.ScrollbarLineButton}">
                                    <Path Data="M 1,0 L 5,4 L 1,8 Z" Fill="{TemplateBinding Foreground}"/>
                                </RepeatButton>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="{x:Type ScrollViewer}">
        <Setter Property="Background" Value="{DynamicResource Gar.Brush.WindowBackground}"/>
    </Style>

    <Style TargetType="{x:Type ToolTip}">
        <Setter Property="Background" Value="{DynamicResource Gar.Brush.ToolTipBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource Gar.Brush.ToolTipText}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Gar.Brush.ControlBorder}"/>
    </Style>

    <Style TargetType="{x:Type Separator}">
        <Setter Property="Background" Value="{DynamicResource Gar.Brush.ControlBorder}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Separator}">
                    <Border Background="{TemplateBinding Background}"
                            MinHeight="1"
                            MinWidth="1"
                            Height="{TemplateBinding Height}"
                            Width="{TemplateBinding Width}"
                            SnapsToDevicePixels="True"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
