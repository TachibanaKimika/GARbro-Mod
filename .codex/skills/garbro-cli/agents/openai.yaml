interface:
  display_name: "GARbro CLI"
  short_description: "Plan, extract, resume, and batch-convert GARbro resources"
  default_prompt: "Use $garbro-cli to validate this resource, plan a safe extraction, and preserve machine-readable provenance."
